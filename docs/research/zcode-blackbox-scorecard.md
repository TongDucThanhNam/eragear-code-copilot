# ZCode Black-Box Scorecard

Date: 2026-06-12

Scope: product-workflow comparison only. ZCode was launched as an end-user app
from `C:\Program Files\ZCode\ZCode.exe`. Observation was limited to visible app
behavior, window/process tree shape, child commands, listener state, and
screenshots. No ZCode archives, binaries, app package contents, private assets,
secrets, tokens, or private config contents were opened, copied, or unpacked.

Status: Eragear has achieved parity for the tracked black-box workflow
scorecard rows. Core Electron flows and the current typecheck/build/smoke
verification set pass, and every `Remaining Eragear Work` cell is now `N/A`.
This means scorecard parity, not a claim that Eragear is a byte-for-byte ZCode
clone or that future product depth work is exhausted.

Evidence:

- ZCode screenshot:
  `docs/research/screenshots/zcode-electron-first-screen.png`
- ZCode sanitized metadata:
  `docs/research/screenshots/zcode-electron-blackbox-metadata.json`
- Eragear Electron first-screen screenshot:
  `docs/research/screenshots/eragear-electron-first-screen.png`
- Eragear dev launch logs:
  `docs/research/screenshots/eragear-dev-desktop-after-zcode-e2e.stdout.log`
  and
  `docs/research/screenshots/eragear-dev-desktop-after-zcode-e2e.stderr.log`

## Black-Box Observation

- ZCode opened a native Electron desktop window titled `ZCode`.
- The latest observed process tree had 9 new ZCode-owned processes: Electron
  main, renderers, GPU, crashpad, network service, Node service, and ACP child.
- The Node service spawned a visible ACP child command for the current
  workspace: `zcode-acp-glm-eragear-code-copilot acp`.
- No ZCode-owned local `Listen` TCP socket was observed in the launch sample.
- The ZCode processes started for the sample were closed after observation.

## Workflow Scorecard

| Workflow | ZCode Black-Box Behavior | Eragear Electron Status | Remaining Eragear Work |
| --- | --- | --- | --- |
| Desktop runtime | Native Electron shell with local agent runtime process tree. | Electron shell uses renderer `electron-ipc` into the private `desktop-service`; no local apps/server HTTP bridge is the desktop default. Electron main now defines an explicit preload-only bridge posture with `contextIsolation:true`, `nodeIntegration:false`, private non-network `desktop-service` transport, redacted local auth token handling, and a renderer CSP header. Runtime diagnostics carry a structured `securityPosture` with CSP, isolation, Node integration, sandbox, endpoint exposure, and token-redaction state; the Local ADE Runtime Strip renders that as a visible Security Posture tile. Dev Vite mode is intentionally classified as `development-warning` because the dev renderer still needs `unsafe-eval`, while the same diagnostic path can report `hardened` when CSP is enforced. Desktop smoke emits `RUNTIME_SECURITY_POSTURE`, `DesktopRuntimeHost` unit coverage verifies bootstrap diagnostics do not leak the local token, and `dev:desktop` logs `securityPosture:'development-warning'` from Electron main while loading the renderer through the private service path. | N/A |
| First screen | Opens into an ADE-style workspace with agent workflow controls. | First screen is Local ADE Control Center with a workspace command deck and active-session cockpit above the Workspace Run Loop / Workflow Readiness surface. The command deck derives operating status, primary action, secondary actions, command chips, and Operation / Guardrail / Tooling / Context panels from live runtime diagnostics plus the Local ADE snapshot; changed files promote checkpoint capture, active sessions surface as the operating phase, and no-provider setup routes to provider configuration instead of a disabled probe. The cockpit derives active chat count, stored session count, pending permissions, active tool calls, subscribers, current model, pid, and attention-first active-session ordering from the same snapshot; when rendered from the chat empty state, Open Chat selects the live chat id through the existing chat shell instead of creating a duplicate session. Its Command Launcher converts `/index <query>`, `/memory <request>`, and `/agent-*` chips into concrete chat text only after required arguments are present, then queues the command for the active or newly created session and submits through the existing chat `handleSubmit` path once connected. The action stack keeps real handlers in the first viewport for start session, open/inspect active chat, command launch, provider probe/config, MCP probe/inspect, checkpoint capture, index refresh, and copyable `/index`, `/memory`, and `/agent-*` commands. Desktop smoke emits `ADE_COMMAND_DECK`, `ACTIVE_SESSION_COCKPIT`, and `LOCAL_ADE_COMMAND_LAUNCH` from the private `desktop-service` path. The run loop still includes a tested Next Actions rail, Active Workspace/Workspace Standby focus area, and readiness lanes for session, provider, MCP, change trust, project context, and subagent state before the deeper runtime health, CLI/provider status, capabilities, memory, MCP, checkpoints, ACP Activity, logs, and dashboard parity sections. | N/A |
| Agent session loop | Local ACP child process participates in agent execution. | Desktop smoke verifies create session, subscribe, send message, observe assistant activity, active-session model switch, subagent command submission, and stop session through the private `desktop-service` path. The first-screen cockpit now includes an Agent Launch selector derived from configured agents, runtime CLI availability, and provider readiness; it calls the existing `initChat(agentId)`/`session.create` path instead of a separate launcher. Start is enabled only for configured agents whose CLI is available and whose provider is not missing/unavailable, while installed-but-unprobed agents are shown as `needs-probe` and missing CLIs are disabled with remediation. This means the loop is no longer hardcoded to the active/default agent: any configured Claude, Codex, Gemini, OpenCode, or custom ACP agent can be selected when its executable/provider state is available. On this machine Claude and Gemini remain honest `missing-cli` diagnostics because those CLIs are not installed. Desktop smoke emits `AGENT_LAUNCH_MATRIX` plus `SESSION_CREATED`, `SESSION_STATE`, `MESSAGE_SENT`, and `SESSION_STOPPED`. | N/A |
| Provider control | Provider/model management is a visible product surface. | Provider table shows agent/provider mapping, redacted env key names, CLI readiness, auth readiness, model readiness, discovered model identifiers, persisted health, readiness remediation, and the runtime default model. OpenCode readiness is verified through real CLI/auth/model probes, Codex readiness uses real `codex doctor --json` output to classify CLI/auth/model separately and surface the configured model without exposing secrets, Claude readiness uses `doctor --json` when available before falling back to safe auth/model commands, and Gemini readiness falls back through safe auth/model commands when doctor JSON is unavailable. The table distinguishes readiness-probed model lists from fallback placeholders, lets users select a discovered model through `settings.selectProviderModel`, can clear the default-model override, and shows the first remediation action separately from diagnostics without exposing secret values. The same control surface now shows active-session model selectors from live ACP config/model state and calls the existing `setModel` mutation; desktop smoke switches a real OpenCode session from `opencode/big-pickle` to `opencode/deepseek-v4-flash-free` and observes the updated Local ADE snapshot. Desktop smoke also creates a temporary Codex provider descriptor when needed, verifies the real CLI through `desktop-service`, selects the doctor-reported `gpt-5.5` model as the runtime default, then removes the descriptor and restores provider/default-model state. Server tests verify Claude/Gemini provider probes and persisted remediation guidance, and desktop smoke emits `PROVIDER_REMEDIATION_MATRIX` from the private `desktop-service` path. | N/A |
| MCP management | MCP is a visible managed-tool workflow. | MCP entries can be added, toggled, initialized, retried per server, discovered, and manually invoked; stdio, streamable HTTP, and SSE-with-message-endpoint paths call MCP `initialize`, `tools/list`, and `resources/list`, with protocol errors shown in UI. Electron shows discovered tools/resources with Run/Read controls; `settings.invokeMcpTool` calls `tools/call`, and `settings.readMcpResource` calls `resources/read` with bounded structured content plus redacted text/JSON previews. Tool/resource invocations persist bounded redacted audit history in `.eragear/mcp-servers.json`, and Electron renders recent invocation audit entries. Chat also exposes `/mcp` when an enabled trusted initialized server has discovered tools; the command resolves `tool`, `server/tool`, or `--server`, calls `settings.invokeMcpTool`, and sends the redacted result through the normal agent message path. Trusted project-local MCP servers are now also injected into ACP `session/new`/`loadSession` setup after the current fingerprint matches Local ADE trust; untrusted, changed, or header-policy-blocked servers are skipped. Trusted project-local stdio, streamable HTTP, and SSE routes are injected as Eragear stdio MCP broker commands instead of raw/native server commands; the broker reloads project config before each tool/resource request, blocks disabled/untrusted/changed fingerprints, resolves remote header-env values inside the broker process, forwards allowed JSON-RPC, redacts response/audit text, and writes `.eragear/mcp-agent-audit.jsonl`. The server build packages the broker into `dist/runtime`, and the resolver supports dev source, built dist, and explicit environment override paths. Electron now shows an Agent Session Routing panel that classifies each project-local MCP route as brokered, blocked by trust/config/header policy, or skipped while disabled, with redacted manifest status, brokered call counts, latest brokered call, and header-env key names only. Manual invocation requires trusting the current redacted server fingerprint through `settings.trustMcpServer`; untrusted or changed fingerprints block execution before protocol calls and demote the MCP capability until reviewed. SSE entries expose a separate message endpoint field. Remote HTTP/SSE headers use env-key mapping, reject literal secret-looking headers, report missing env keys by name, and expose only redacted present/missing metadata. Each probe returns a sanitized step timeline for header policy, executable resolve/spawn, stream open, endpoint resolution, initialize, initialized notification, tools/list, and resources/list where applicable. SSE discovery probes now reconnect once and replay pending discovery JSON-RPC requests when the stream drops before protocol discovery completes. SSE invocation replays safe `resources/read` once after stream loss; side-effecting `tools/call` is not replayed automatically and returns an exact policy diagnostic. Explicit `settings.probeMcpServer` runs persist bounded redacted probe history for reload/debug review. Server-pushed JSON-RPC notifications without `id` are captured during probe, invocation, and the new trusted SSE Watch action, classified as `probe`, `invocation`, or `monitor`, redacted, persisted as bounded history, and rendered in Electron. The Watch action performs MCP `initialize`, sends `notifications/initialized`, holds the SSE stream for a bounded interval, reconnects once on stream loss, persists a separate notification monitor run with stream/reconnect/count diagnostics, and desktop smoke emits `MCP_NOTIFICATION_MONITOR`. Remote HTTP/SSE entries now expose per-server Remote Controls in Electron for request timeout, reconnect attempts, and default notification watch duration; `settings.configureMcpRemoteControls` persists those controls, includes them in the trust fingerprint, changes trust status until re-approved, applies timeout/reconnect/watch values to probe, invocation, watch, and agent-broker paths, and desktop smoke emits `MCP_REMOTE_CONTROLS` plus a `MCP_NOTIFICATION_MONITOR` run using the configured watch duration. | N/A |
| Capabilities | Skills, commands, agents, MCP, hooks, plugins are surfaced as product concepts. | Registry lists discovered skills/commands/output styles/memory/MCP/providers/hooks/plugins. Project slash commands are discovered from Markdown and `/command args` expands into a real chat prompt before `sendMessage`. The built-in `/index <query>` command searches Project Index and sends bounded index context through the normal chat path, and normal chat prompts can auto-attach top Project Index matches when the index is ready. Project Memory now has real `/memory <request>`, `/memory --source <path> <request>`, and `/memory --preset <id>` chat paths through `settings.buildProjectMemoryContext`; normal prompts can auto-attach enabled memory with server-side redaction, Electron can save/delete project-local memory presets with selected sources/default query/budget, and the chat action menu exposes source and preset pickers that insert the same command path. Trusted discovered MCP tools are manually invokable from chat through `/mcp`, with JSON-object arguments and the MCP result forwarded to the agent as a normal message. Skills are invokable through `@skill-name` or `/skill-*`; output styles are invokable through `/style-*`. Subagents are discovered from Markdown descriptors and invokable through `/agent-*` chat commands; desktop smoke verifies `/agent-code-reviewer` appears and submits through the normal chat send path after prompt expansion. Manual hooks are executable from Electron, lifecycle hooks now cover project-index, checkpoint, and agent session create/send/stop events, and project-local plugins can be saved/toggled/trusted/run with redacted audit output. Hook and plugin capabilities activate only after the current execution or permission fingerprint is trusted and `executionPolicy` allows the command, manual Run requires a per-descriptor confirmation token plus a one-shot approved operation fingerprint before spawn, direct shell-eval commands are blocked before spawn, run history has review/reopen state, all/open/reviewed filtering, and redacted JSON export, env-key allowlists are shown without exposing secret values, and execution policy presets are persisted/enforced: hook `restricted` blocks manual runs while keeping lifecycle hooks available, hook/plugin `blocked` rejects before spawn, and plugin `restricted` forces sandbox workspace access even when project-root is requested. Hook lifecycle governance is now persisted/enforced with lifecycle enabled/paused state, per-event pauses, `continue` or `stop-on-failure`, shared batch ids, and disabled-run diagnostics. Hook/plugin run scheduling is now persisted/enforced with enabled/paused state, max concurrent run slots, per-item cooldown, per-item `ready`/`paused`/`cooldown`/`parallel-limit` status, capability demotion, and disabled audit rows before spawn when scheduling blocks execution. Hook batch queue can run up to eight ready hooks after `RUN HOOK BATCH`, rechecks operation fingerprints, applies trust/policy/scheduling gates per member, supports `continue` and `stop-on-failure`, persists `hook-batch-*` summaries, and shows recent batch results in Electron. Plugin batch queue can run up to eight ready plugins after `RUN PLUGIN BATCH`, rechecks operation fingerprints, applies trust/permission/policy/expiry/scheduling gates per member, supports `continue` and `stop-on-failure`, persists `plugin-batch-*` summaries, persists reusable batch presets with `lastRunBatchId`, persists per-plugin `dependencyIds`, exposes a `dependencyGraph`, topologically orders selected batch dependencies, persists due schedules for batch presets with saved operation fingerprints, dispatches due schedules through the server `plugin-batch-schedule-dispatch` background task, exposes that daemon on `runtime.background` and the first-screen Background Task Fleet, and shows recent batch results, dependency graph status, schedule status, plus save/run/delete preset controls in Electron. Hook and plugin process isolation now persists structured `job-process-tree` metadata, spawns with `shell:false`, uses taskkill `/T /F` on Windows or detached process-group termination on Unix-like hosts for timeouts, records process-tree termination diagnostics on run history, and renders isolation posture in Electron. Plugins without `project-root` scope run in a temporary sandbox cwd with `ERAGEAR_PROJECT_ROOT` hidden, project-root plugin runs persist checkpoint-backed workspace audit, plugin permission grant/revoke uses a separate scopes/env/workspace fingerprint, project-local signed plugin packages are discovered from `.eragear/plugin-packages`, verified with Ed25519 signatures, classified as installable/installed/update/invalid, and installed from Electron with publisher/signature/public-key plus publisher identity and expiry metadata shown. Expired signed packages are rejected, already-installed signed plugins demote/block manual run when their stored package expiry passes, and signed packages can be revalidated from Electron against local manifest or registry pins; tampered or untrusted package provenance persists `verification-failed`, demotes the plugin capability, and blocks later approval/run. Saved signed registries can be added from Electron, trusted by URL fingerprint, refreshed into pinned package metadata and registry-fed `revokedSigners`, used for install/update through `settings.installPluginRegistryPackage` after signature/public-key hash plus optional publisher identity/issue/expiry pin verification, and guarded by registry trust revocation plus manual or feed-managed signer revocation. | N/A |
| Hooks | ZCode exposes hooks as part of local ADE capability surface. | Hook Runner v0 saves project-local hooks in `.eragear/hooks.json`, derives a `sha256:` execution fingerprint from command/args/cwd/event/env-key allowlist plus policy preset, requires trust approval before run, demotes untrusted, changed, or sandbox/policy-blocked hooks from active capabilities, requires a per-hook `RUN HOOK <id>` confirmation token plus a one-shot approved operation fingerprint for manual Electron runs, consumes the approval after execution, runs commands without shell expansion from a project-root-guarded cwd, blocks direct shell-eval interpreter commands through `executionPolicy`, passes only a small base process env plus approved env keys and Eragear hook context, redacts secret-looking output, persists recent run history, and shows trust/run/sandbox/policy results, run-operation approval status, isolation posture, and open/reviewed audit state in Electron. Electron exposes an Approve Run action, policy preset control, Run Scheduling controls, and a Hook Batch Queue panel. `standard` keeps the normal trust/approval flow, `restricted` blocks manual hook approval/run while lifecycle execution remains available, and `blocked` rejects execution before spawn. The server blocks `settings.runHook` before spawn when the current run operation approval is missing, changed, expired, consumed, or policy-blocked. `settings.runHookBatch` requires `RUN HOOK BATCH`, rechecks each selected hook operation fingerprint immediately before execution, applies trust/policy/scheduling gates per member, supports `continue` and `stop-on-failure`, records disabled or failed audit rows without spawn when blocked, and persists `hook-batch-*` summaries with counts/run ids/diagnostics. The Run Audit section filters all/open/reviewed runs and copies a server-backed redacted JSON artifact through `settings.exportHookRuns`. Lifecycle events execute for `after-project-index-refresh`, `after-checkpoint-create`, `after-checkpoint-restore`, `after-agent-session-create`, `after-agent-message-send`, and `after-agent-session-stop`; agent-session hooks receive bounded chat/session/turn context. Lifecycle governance persists enabled/paused state, per-event pauses, and `continue` versus `stop-on-failure`; each lifecycle dispatch records a shared `hook-batch-*` id, paused events create disabled audit rows without spawn, and failed stop-on-failure batches disable later hooks in the same batch. Electron exposes those controls and batch diagnostics. Hook run scheduling persists enabled/paused state, max concurrent run slots, and per-item cooldown; manual, batch, and lifecycle runs create disabled audit rows before spawn when scheduling blocks execution. Hook process isolation persists structured `job-process-tree` metadata on policy, run-operation, and run records; timeouts terminate the process tree with Windows `taskkill /T /F` or Unix-like detached process-group termination, and run diagnostics record termination outcome. Desktop smoke verifies `HOOK_RUN_APPROVAL`, consumed operation state after run, `HOOK_RUN_REVIEW`, `HOOK_RUN_AUDIT_EXPORT`, `HOOK_SCHEDULING_POLICY`, `HOOK_BATCH_QUEUE`, `HOOK_PROCESS_ISOLATION`, `HOOK_SANDBOX_BLOCK`, `HOOK_POLICY_PRESET`, and `HOOK_LIFECYCLE_GOVERNANCE` through the private service. Unit tests verify hook batch fingerprint guards and timeout child-process tree termination. | N/A |
| Plugins | ZCode exposes extension/plugin concepts as part of local ADE capability surface. | Plugin Runner v0 saves project-local plugins in `.eragear/plugins.json`, derives a `sha256:` execution fingerprint from command/args/cwd/scopes/env-key allowlist plus policy preset, derives a separate permission fingerprint from effective scopes/env-key/workspace access/policy preset, requires command trust and permission grant before execution, demotes untrusted, permission-missing, changed, expired-package, failed-package-governance, sandbox-blocked, policy-blocked, or scheduling-blocked plugins from active capabilities, requires a per-plugin `RUN PLUGIN <id>` confirmation token plus a one-shot approved operation fingerprint for manual Electron runs, runs commands without shell expansion from a project-root-guarded cwd only when effective `project-root` scope is granted, blocks direct shell-eval interpreter commands through `executionPolicy`, passes only base process env plus approved env keys and Eragear plugin context, redacts secret-looking output, persists recent run history, and shows workspace access, policy preset, permission status, operation approval status, process isolation posture, package governance status, scopes/env-key names, scheduling status, results, sandbox status, workspace audit, and open/reviewed audit state in Electron. Electron exposes plugin permission Grant/Revoke controls, a policy preset control, signed package Revalidate, an Approve Run action, Run Scheduling controls, and a Batch Queue panel for ready plugins plus reusable presets, dependency graph status, and due schedule controls with `continue` or `stop-on-failure`. `standard` keeps the normal trust/permission/approval flow, `restricted` forces sandbox workspace access even when project-root is requested, and `blocked` rejects approval/run before spawn. The server blocks `settings.runPlugin` before spawn when the current permission fingerprint is missing/changed, the current run operation approval is missing/expired/consumed, policy blocks execution, signed package governance failed, or scheduling is paused/cooling down/slot-limited; `settings.runPluginBatch` requires `RUN PLUGIN BATCH`, rechecks each operation fingerprint immediately before execution, applies trust/permission/policy/expiry/package-governance/scheduling gates per member, records disabled or failed audit rows, supports `stop-on-failure` by creating disabled skip rows for remaining requested plugins without spawning them, orders selected dependencies before dependents, creates disabled audit rows for missing selected dependencies, cycles, or failed dependencies before spawn, and persists `plugin-batch-*` summaries with run ids, counts, diagnostics, plugin names, and failure mode; `settings.runPluginBatchPreset` resolves a saved preset into that guarded dependency-aware path and updates preset `lastRunBatchId`; `settings.upsertPluginBatchSchedule`, `settings.runDuePluginBatchSchedules`, and `settings.deletePluginBatchSchedule` persist schedules for saved presets, run due schedules through the same guarded batch path, and stale saved fingerprints produce disabled audit rows before spawn. The server BackgroundRunner now registers `plugin-batch-schedule-dispatch`, which scans the local desktop user plus active session users for due schedules and delegates back through `settings.runDuePluginBatchSchedules`; Local ADE snapshots expose that task fleet on `runtime.background`, and Electron renders the first-screen Background Task Fleet with cadence, timeout, status, counts, duration, and last result. Desktop smoke verifies `PLUGIN_PERMISSION_GRANT` with revoke, run rejection, re-grant, capability reactivation, `PLUGIN_RUN_APPROVAL`, consumed operation state after run, `PLUGIN_PROCESS_ISOLATION`, `PLUGIN_SCHEDULING_POLICY`, `PLUGIN_DEPENDENCY_GRAPH`, `PLUGIN_BATCH_QUEUE`, `PLUGIN_BATCH_STOP_ON_FAILURE`, `PLUGIN_BATCH_PRESET`, `PLUGIN_BATCH_SCHEDULE` with `daemon:true` plus visible task result metadata, and `PLUGIN_POLICY_PRESET` with requested project-root access forced to sandbox. Signed package catalog scans project-root-local `.eragear/plugin-packages/**/*.json` manifests with `schemaVersion`, `publisher`, optional `publisherId`, optional `issuedAt`, optional `expiresAt`, `publicKeyPem`, `signature`, and `plugin`, verifies Ed25519 signatures over canonical manifest payloads, classifies each package as installable/installed/update/invalid with sanitized diagnostics, rejects tampered manifests, expired manifests, future issuedAt values, and out-of-root paths, stores publisher/signature/public-key hash plus publisher identity and issue/expiry metadata, and grants the verified command/permission fingerprints before the approval-gated run path. Saved registry management persists `.eragear/plugin-registries.json`, requires trusting the current registry URL fingerprint before refresh/install, stores refreshed package pins, diagnostics, optional publisher identity/issue/expiry pins, and registry-fed `revokedSigners`, classifies packages as installable/installed/update-available/invalid/revoked, installs or updates registry packages from Electron through `settings.installPluginRegistryPackage` after rechecking signature, public-key hash, and optional publisher identity/issue/expiry pins, supports revoking URL trust, supports manual signer revocation/restore, and treats feed-managed signer revocations as non-restorable until the registry clears them. The direct `registryUrl + packageId` install path also rejects packages revoked by the registry feed or expired by registry/package policy before manifest execution. `settings.revalidatePluginPackage` re-reads an installed signed plugin's local manifest or trusted registry pins, verifies signature/public-key/publisher/issue/expiry/plugin identity, persists `verified` or `verification-failed` diagnostics, demotes failed plugin capabilities, and blocks later approval/run until the package is reinstalled or revalidated successfully. Process-only and policy-restricted plugins run in a temporary sandbox cwd, receive `ERAGEAR_PLUGIN_WORKSPACE_ACCESS=sandbox`, do not receive `ERAGEAR_PROJECT_ROOT`, and clean up the sandbox cwd after execution. Plugin process isolation persists structured `job-process-tree` metadata on policy, run-operation, and run records; timeouts terminate the process tree with Windows `taskkill /T /F` or Unix-like detached process-group termination, and run diagnostics record termination outcome. Project-root plugins capture before/after Git status, create an `apply-patch` pre-run safety checkpoint when the workspace is already dirty, create a `reverse-patch` post-run change checkpoint when changes remain, and persist checkpoint ids plus changed-file summaries on the run record. The Run Audit section filters all/open/reviewed runs and copies a server-backed redacted JSON artifact through `settings.exportPluginRuns`. Desktop smoke verifies `PLUGIN_PERMISSION_GRANT`, `PLUGIN_RUN_APPROVAL`, `PLUGIN_PROCESS_ISOLATION`, `PLUGIN_SCHEDULING_POLICY`, `PLUGIN_DEPENDENCY_GRAPH`, `PLUGIN_BATCH_QUEUE`, `PLUGIN_BATCH_STOP_ON_FAILURE`, `PLUGIN_BATCH_SCHEDULE`, `PLUGIN_CATALOG`, `PLUGIN_SIGNED_INSTALL` with publisher identity/expiry metadata and consumed operation approval, `PLUGIN_PACKAGE_REVALIDATION` with tamper demotion, `PLUGIN_REGISTRY_INSTALL` including identity/expiry pins plus trust/manual-signer/feed-signer revocation checks and consumed operation approval, `PLUGIN_RUN_REVIEW`, `PLUGIN_RUN_AUDIT_EXPORT`, `PLUGIN_SANDBOX_BLOCK`, `PLUGIN_WORKSPACE_SANDBOX`, `PLUGIN_POLICY_PRESET`, and `PLUGIN_WORKSPACE_AUDIT` through the private service without leaking the allowed plugin secret or writing into the project root. Unit tests verify signed package revalidation failure blocks approval/run and plugin timeout child-process tree termination. | N/A |
| Project memory | Project context is surfaced as part of agent workflow. | `AGENTS.md`, `CLAUDE.md`, `.eragear/memory.md`, and `.eragear/context.md` are detected, previewed, redacted for secret-looking lines, and toggleable. `settings.buildProjectMemoryContext` reads enabled sources on the server, applies a bounded context budget, redacts secret-looking values, and returns an agent prompt. It supports full-source context and semantic chunk retrieval; semantic mode splits sources into bounded line-aware chunks, calls a configured OpenAI-compatible embedding endpoint via `ERAGEAR_EMBEDDINGS_ENDPOINT`/`ERAGEAR_EMBEDDINGS_MODEL` when available, ranks chunks by model embedding cosine similarity, records ranker/model/dimensions metadata, and preserves redaction before prompt construction. If the endpoint is unavailable or unconfigured, diagnostics explicitly report the fallback to local token-vector ranking instead of presenting it as model-backed retrieval. Chat exposes `/memory <request>`, `/memory --source <path> <request>`, `/memory --preset <id>`, and `/memory --semantic --chunks <n> <request>`; the action menu has visible Project Memory source/preset pickers plus a Best Matching Chunks command; normal prompts auto-attach semantic Project Memory when eligible; and if Project Index also attaches, the renderer composes both contexts before `sendMessage`. Electron can save/delete project-local presets with selected sources, default query, budget, retrieval mode, and chunk count in `.eragear/project-memory-presets.json`; desktop smoke verifies selected-source context, `PROJECT_MEMORY_PRESET`, `PROJECT_MEMORY_SEMANTIC`, and `PROJECT_MEMORY_MODEL_EMBEDDING` through `desktop-service` with redacted `api_key` and no embedding secret leakage. | N/A |
| Project index | ZCode maintains deeper local workspace state during ADE operation. | Project Index can be refreshed from Electron, writes `.eragear/repo-index.json`, skips generated/dependency/checkpoint directories, and shows file count, byte total, extension summary, diagnostics, indexed file metadata, bounded code symbols, TODO/FIXME/HACK/BUG/XXX task markers, local semantic token-profile readiness, and model embedding metadata when configured. Refresh now prioritizes root/depth and signal-rich files for embedding quota, calls a configured OpenAI-compatible embedding endpoint, stores redacted bounded-excerpt vectors plus model/dimensions/hash metadata in `.eragear/repo-index.json`, and strips raw vectors from the Electron snapshot. `settings.searchProjectIndex` ranks files/symbols/tasks plus semantic profile hits and stored model vectors; query embedding compares against indexed vectors and model-only hits are marked as `matchKind:"embedding"`. `/index <query>` sends a bounded retrieval prompt to the agent, and normal chat prompts can auto-attach bounded top matches when the index is ready. Electron's Project Index tile shows model embedding source/model/dimensions when available. Desktop smoke verifies `PROJECT_INDEX_SEMANTIC_SEARCH` and `PROJECT_INDEX_MODEL_EMBEDDING` by finding `desktop-semantic-smoke.md` for the conceptual query `rollback safety` through the private `desktop-service` path, with raw vectors hidden from the visible snapshot and no embedding secret leakage. | N/A |
| Checkpoints/change trust | Checkpoint/change review is a visible trust workflow. | Git diff fallback, checkpoint creation, patch storage, structured side-by-side diff preview, session-turn attribution, per-file restore risk matrix, restore readiness checks, confirmation token, guarded full restore, guarded selected-file restore, guarded selected-hunk restore, partial restore history, and automatic safety checkpoints are implemented. Attribution shows active chat id, agent session id, agent label, chat status, message count, latest message preview, and active/completed turn ids where available. Restore risk shows safe/warning/blocked state, patch action, checkpoint/current status, and reason. Electron now derives a safe restore plan from the risk matrix, preselects safe patch-backed files, and exposes Select Safe/Clear controls so selected-file restore remains usable when an unrelated blocked file prevents full restore. It also exposes a Mixed Restore editor above the raw diff: safe files can be selected, all hunks in a file can be selected or cleared, shelvable blockers can be moved one file at a time, and tracked same-file conflicts expose per-file Keep, Restore Side, and selected-hunk Apply Conflict Hunks actions guarded by the same restore token. The server revalidates shelved blockers before moving them into `.eragear/checkpoint-shelves`, records `conflictShelves`, and filters shelf artifacts out of restore preconditions. Tracked same-file patch conflicts are detected by per-file `git apply --check`; Keep Current records the conflict decision and omits that file from later full-restore patch/precondition checks, Use Restore Side creates an apply-patch safety checkpoint before resetting the file, and hunk choices restore selected conflict hunks while recording `mixed` per-hunk `restore`/`current` choices plus a selected-hunk safety checkpoint. Selected-file restore filters the patch to exact file sections and preserves unrelated workspace changes. Selected-hunk restore filters the patch to selected `@@` hunk blocks, creates a hunk-scoped safety checkpoint, and preserves other hunks in the same file. Electron now renders a Visual Merge section with restore-mode-specific current/restore labels, per-file risk/recommended-action badges, file and hunk selection controls, and side-by-side row-level current/restore cells above the raw structured diff. Desktop smoke verifies `CHECKPOINT_TRACKED_CONFLICT_RESOLVE`, `CHECKPOINT_MIXED_CONFLICT_EDITOR`, `CHECKPOINT_TRACKED_CONFLICT_CHOICE`, `CHECKPOINT_CONFLICT_HUNK_CHOICES`, and `CHECKPOINT_VISUAL_MERGE`. | N/A |
| Logs/observability | Runtime activity is treated as a product concern. | Runtime timeline is visible in Electron and backed by local log store queries. ACP Activity is also visible from Electron: desktop-service mirrors structured ACP events into the log store, the snapshot filters to the current user or owned chats, aggregates level/kind/chat counts, exposes event kind and payload byte counts, builds chat/session/turn/source correlation summaries, and strips all `rawPayload*` metadata before rendering. Electron can call `settings.exportAcpActivity` to copy a schema-versioned redacted trace plus those correlation summaries and the cross-session timeline, and can call `settings.replayAcpActivity` to load chronological redacted replay frames with stable sequence, elapsed/delta timings, correlation key/label, safe metadata, correlation filtering, workspace-wide replay, and server-side kind filtering. The ACP Activity panel exposes a Cross-session timeline with chat/source lanes, lane hops, frame rows, per-lane replay buttons, Workspace replay, Play/Pause, Prev/Next, per-correlation replay controls, Replay by kind controls, and project-local saved replay presets backed by `.eragear/acp-replay-presets.json`. It now also exposes Stream diagnostics with a real `settings.retryAcpActivityStream` Retry Stream action, retry delay/max attempts, heartbeat/stale thresholds, latest age, max silence/average delta/gap thresholds, gap rows, causality root/correlated/orphan counts, longest chain length, and per-chain replay controls; the same redacted stream diagnostics are included in snapshot/export data without replaying side-effecting protocol calls. Desktop smoke verifies `ACP_CROSS_SESSION_TIMELINE`, `ACP_STREAM_DIAGNOSTICS`, `ACP_STREAM_RETRY`, `ACP_REPLAY_KIND_FILTER`, and `ACP_REPLAY_PRESET` through `desktop-service` with redacted replay frames, 11 chat lanes, 30 lane transitions, `workspaceChatCount:8`, retry policy `1000ms x5`, causal chains, and no raw payload metadata. | N/A |
| Dashboard parity | Local ADE functions are available in-app. | Local runtime, agents, sessions, logs, memory, MCP, providers, capabilities, and checkpoints have Electron paths. Remote auth admin/device-session management is now an explicit Local ADE policy item, not a hidden blocker: `dashboardParity` classifies it as `not-applicable` for the `local-desktop` scope with a reviewed rationale, Electron renders that policy in the Dashboard Parity rail, and `blockers` no longer includes it. Desktop smoke emits `DASHBOARD_LOCAL_POLICY` with `decision:"not-applicable"` and `inBlockers:false` from the private `desktop-service` path. | N/A |

## Comparison Note

ZCode's black-box launch confirms a serious Electron ADE shape: native window,
local runtime delegation, ACP child process, and visible workspace flow. Eragear
now exercises the same core local-ADE categories with its own implementation:
private Electron IPC transport, OpenCode and Codex doctor-backed provider readiness probes plus validated provider model selection/clear and active-session model switching, MCP protocol
discovery plus panel and `/mcp` chat manual tool/resource invocation with redacted persisted audit,
bounded SSE discovery reconnect/replay plus safe SSE resource invocation
reconnect/replay and side-effecting tool no-replay policy,
trusted project-local MCP injection into ACP session setup plus stdio/HTTP/SSE broker enforcement/audit and redacted Agent Session Routing preview,
fingerprint trust gating, redacted MCP notification history, and Electron-visible remote MCP timeout/reconnect/watch controls that are included in trust fingerprints and verified through agent broker/session setup,
checkpoint safety restore with safe selected-file planning, guarded
untracked blocker shelving, tracked-conflict keep-current/restore-side choices,
and tracked-conflict per-hunk mixed choices,
memory/capability actions, local slash command invocation, manual skill invocation,
output-style invocation, manual
subagent invocation, trusted env-scoped manual plus governed lifecycle hook
execution with batch ids, per-event pause, and stop-on-failure across
project/checkpoint and agent session events,
trusted env-scoped project-local plugin execution with process-only sandbox cwd,
project-root scope enforcement, separate permission grant/revoke, one-shot
operation approval before manual hook/plugin run, guarded plugin batch queue with
per-member fingerprint rechecks, dependency graph ordering, stop-on-failure skip auditing, reusable preset execution, and due schedule execution for saved presets, signed package catalog/install plus
publisher identity/expiry policy, saved trusted registry refresh/install/update
with identity/expiry pins, and checkpoint-backed workspace audit,
metadata plus code-symbol/task indexing,
manual `/index` retrieval into chat, source-selective, preset-backed, and local semantic chunk server-built `/memory` project context plus chat pickers,
automatic metadata/semantic-profile Project Index plus semantic Project Memory context for normal prompts,
redacted ACP Activity plus redacted trace export/replay, correlation summaries,
cross-session timeline lanes/transitions, workspace-wide replay, kind-filtered
replay, and saved replay presets in the Electron control surface,
a workflow-readiness
first screen with a live command deck, Active Workspace focus, and real Next
Actions routing,
and a verified agent session loop. The first viewport now adds a tested ADE
command deck plus active-session cockpit with operating phase, primary action,
secondary action stack, guardrail/tooling/context panels, live session counts,
permission/tool-call/subscriber totals, current model, Open Chat routing, and
placeholder-safe Command Launcher routing into the normal chat submit path
instead of only diagnostics,
and the renderer now passes
`apps/web check-types` after resolving its stale React/Vite type drift. The
scorecard now has zero remaining tracked workflow blockers; future work can
continue as product depth beyond this black-box parity checklist rather than as
open parity blockers.
